<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.snow.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.js"></script>
    <title>POST THE BLOG</title>
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f7fa;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            font-size: 2.5rem;
            color: #333;
            margin-top: 30px;
        }

        /* Form Container */
        .form-container {
            max-width: 800px;
            margin: 30px auto;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Form Styling */
        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        input[type="text"], textarea, input[type="file"] {
            width: 100%;
            padding: 12px;
            margin: 5px 0;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 1rem;
        }

        input[type="text"]:focus, textarea:focus, input[type="file"]:focus {
            outline: none;
            border-color: #007bff;
        }

        /* Heading Styling */
        .heading-input {
            height: 50px;
            font-size: 1.2rem;
        }

        /* Visibility Section */
        .visibility-set {
            font-size: 1rem;
            color: #333;
        }

        .visibility-set li {
            list-style: none;
            margin-bottom: 10px;
        }

        .visibility-set input[type="checkbox"] {
            margin-left: 10px;
        }

        /* Button Styling */
        button[type="submit"] {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 12px;
            font-size: 1.1rem;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s;
            width: 100%;
        }

        button[type="submit"]:hover {
            background-color: #0056b3;
        }

        /* Quill Editor Styling */
        .ql-container {
            height: 300px;
            border-radius: 5px;
            border: 1px solid #ccc;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
            }

            .heading-input {
                font-size: 1rem;
            }

            button[type="submit"] {
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <h1>POST THE BLOG HERE</h1>

    <div class="form-container">
        <!-- Form for Blog Submission -->
        <form action="/create" enctype="multipart/form-data" method="post" id="blogform">
            <!-- Blog Heading -->
            <input type="text" class="heading-input" placeholder="BLOG HEADING" name="heading" required />
            
            <!-- Visibility Set -->
            <div class="visibility-set">
                <h2>VISIBILITY SET</h2>
                <ul>
                    <li>
                        PUBLIC
                        <input type="checkbox" name="public" value="Public">
                    </li>
                    <li>
                        PRIVATE
                        <input type="checkbox" name="private" value="Private">
                    </li>
                </ul>
            </div>

            <!-- Blog Content (Quill Editor) -->
            <textarea name="blogcontent" placeholder="Describe here" required></textarea>

            <!-- Image Upload -->
            <input type="file" name="filenames" multiple>

            <!-- Submit Button -->
            <button type="submit">POST</button>
        </form>
    </div>

</body>

</html>
